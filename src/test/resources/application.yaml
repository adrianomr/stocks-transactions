spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stocks-transactions
    username: postgres
    password: postgres
spring.jpa.hibernate.ddl-auto: validate

amazon:
  accessKey: test
  secretKey: test

server:
  tomcat:
    max-threads: 200
